## Portfolio update — IO as context engine

Here's the project entry following Kinetica AI's portfolio format:

---

**IO — Clinical Context Engine**
Local AI agent with incremental memory (ChromaDB + SQLite) for longitudinal clinical reasoning. Reduces context reconstruction time across sessions by maintaining structured delta logs.
Stack: LangGraph · ChromaDB · SQLite · Ollama · FastAPI

---

Fits under Projects alongside polar-lyme-predictor. Same stack family, complementary scope — one handles biosignal prediction, IO handles session memory and reasoning continuity.

One consideration: IO is internal infrastructure, not a standalone product. The portfolio entry should reflect that — a systems component, not a client-facing tool. Framing it as "clinical context engine" rather than "AI assistant" keeps it technically honest.

#healthtech #clinicalAI #biomechanics